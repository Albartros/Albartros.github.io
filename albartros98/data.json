---
layout: null
---
{
{% for code in site.data.sequences %}
    "{{ code.sequence }}" : {
        "title": "{{ code.title }}",
        "id": "{{ code.id }}"
    }{% if forloop.last %}{% else %},{% endif %}
{% endfor %}
}