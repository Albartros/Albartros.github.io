---
layout: null
---
{
{% for code in site.data.seq_mugen %}
    "{{ code.sequence }}" : {
        "title": "{{ code.title }}",
        "id": "{{ code.id }}"
    }{% if forloop.last %}{% else %},{% endif %}
{% endfor %}
}